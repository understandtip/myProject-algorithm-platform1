<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.project.mapper.FavoriteMapper">
    <resultMap id="forwardResultMap" type="forward">
        <result property="bid" column="bid" />
        <result property="fid" column="fid" />
    </resultMap>

    <!--<update id="update">
        update forward set bid = #{bid},fid = #{fid} where bid = #{bbid} and fid = #{ffid}
    </update>-->

</mapper>